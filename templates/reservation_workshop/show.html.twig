{% extends 'base.html.twig' %}

{% block title %}ReservationWorkshop{% endblock %}

{% block body %}
    <h1>ReservationWorkshop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_workshop.id }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ reservation_workshop.categorie }}</td>
            </tr>
            <tr>
                <th>Date_reservation</th>
                <td>{{ reservation_workshop.dateReservation ? reservation_workshop.dateReservation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reservation_workshop.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ reservation_workshop.createdAt ? reservation_workshop.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ reservation_workshop.updatedAt ? reservation_workshop.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_workshop_index') }}">back to list</a>

    <a href="{{ path('app_reservation_workshop_edit', {'id': reservation_workshop.id}) }}">edit</a>

    {{ include('reservation_workshop/_delete_form.html.twig') }}
{% endblock %}
