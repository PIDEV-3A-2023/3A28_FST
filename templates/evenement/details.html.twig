{% extends 'base.html.twig' %}




{% block title %}ArtStation
{% endblock %}

{% block body %}

<section class="breadcrumbs">
	<div class="container">
<h2 style="  color:white ">Details</h2>

	</div>
</section>


<div class="container">
	<div class="row">
		<div class="col-md-6 offset-md-3">

			<h1>{{ event.titre }}</h1>


					<p>{{ event.description }}</p>
					<p>Date Début:
						{{ event.dateDebut|date('d/m/Y') }}</p>
					<p>Date Fin:
						{{ event.dateFin|date('d/m/Y') }}</p>
					<p>Prix:
						{{ event.prix }}</p>
					<p>nombre de places:
						{{ event.nbPlace }}</p>



					<p>Lieu:
						{{ event.localisation }}</p>
							<a class="btn btn-warning" href="{{ path('feedback',{'id': event.id}) }}">
								<span>donner votre feedback</span>

							</a>
							<a class="btn btn-warning" href="{{ path('update', {'id': event.id}) }}">Update</a>
							<a class="btn btn-danger" href="{{ path('delete', {'id': event.id}) }}">Delete</a>

		
		</div>	
	</div>
</div>


{% endblock %}