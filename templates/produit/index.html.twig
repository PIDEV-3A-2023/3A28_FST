{% extends 'base.html.twig' %}

{% block title %}Art Station{% endblock %}



{% block body %}
    <section id="portfolio" class="portfolio">
      <div class="container" data-aos="fade-up" data-aos-delay="200" style=" height: 1000px; ">

        <div class="section-title">
          <h2>Les produits</h2>
          <div class="row justify-content-center mt-3">
          <div class="col-md-3">
           <form action="{{ path('search') }}" method="GET" class="mt-3" style ="margin-top: 20px; margin-bottom:40px">
            <div class="input-group" style =" width: 300px;">
              <input type="text" name="query" class="form-control" placeholder="Chercher un produit..." style ="
                padding: 6px 12px;
                font-size: 14px;
                line-height: 1.428571429;
                color: #555;
                background-color: #fff;
                background-image: none;
                border: 1px solid #ccc;
                border-radius: 4px;
                -webkit-box-shadow: inset 0 1px 1px rgba(0, 0, 0, .075);
                box-shadow: inset 0 1px 1px rgba(0, 0, 0, .075);">
              <button type="submit" class="btn btn-primary" style="   padding: 6px 12px;
                margin-left:5px;
                font-size: 14px;
                line-height: 1.428571429;
                text-align: center;
                white-space: nowrap;
                vertical-align: middle;
                cursor: pointer;
                background-image: none;
                border: 1px solid #F6B49C;
                border-radius: 4px;
                color: #fff;
                background-color: #F6B49C;"><i class="fa fa-search"></i></button>
            </div>
          </form>
          </div>
          </div>
          </div>
          <div class="row" data-aos="fade-up" data-aos-delay="100">
          <div class="col-lg-12 d-flex justify-content-center">
            <!--div class="d-flex">
                <a href="{{ path('ajoutProduit') }}" class="btn btn mb-2 mr-1 mx-auto" style="width: 40%; background-color: white; color: black; border: 2px solid #455771; box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2), 0 3px 10px 0 rgba(0,0,0,0.19);">Ajouter Produit</a>

                <a href="{{ path('ajoutCategorie') }}" class="btn btn mb-2 ml-1 mx-auto" style="width: 40%; background-color: white; color: black; border: 2px solid #FAC881; box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2), 0 3px 10px 0 rgba(0,0,0,0.19);">Ajouter Categorie</a>
            </div-->
          </div>
        </div>
        <div class="row gy-4 portfolio-container" >
        {% for produit in pagination %}
            <div class="col-md-2 mb-2">
                <div class="card" data-product-id="1234">
                    <img src="uploads/images/{{produit.image}}" alt="{{ produit.nom }}" style="width: 194px; height: 199px; object-fit: cover; border: 1px solid #ccc; box-shadow: 0px 0px 5px #ccc;">
                    <a href="#" class="heart-icon" style ="position: absolute;
                          top: 10px;
                          right: 10px;
                          padding: 5px;
                          color: white;"><i class="fa fa-heart"></i></a>
                    <div class="card-body">
                        <h5 class="card-title">{{ produit.nom }}</h5>
                        <p class="card-text">{{ produit.prix }} DT</p>
                      <div class="container">
                        <div class="row justify-content-between">
                          <div class="col-auto">
                            <a href="#" class="btn cart-btn" style ="  display: inline-block;
                              background-color: transparent;
                              border: 2px;
                              padding: 0;
                              "><i class="fas fa-shopping-cart" style ="color: #F6B49C;"></i></a>
                          </div>
                              <div class="col-auto">
                            <a href="{{ path('app_produit_detail', {'id': produit.id}) }}" class="btn more-details-btn" style ="  display: inline-block;
                              background-color: transparent;
                              border: 2px;
                              padding: 0;
                              "><i class="fas fa-info-circle" style ="color: #F6B49C;"></i></a>
                              </div>
                        </div>
                      </div>
                    </div>
                </div>
            </div>
        {% endfor %}
     <div class ="d-flex justify-content-center">
     
     {{knp_pagination_render(pagination, 'pagination.html.twig')}}
          </div>
        </div>
      </div>
    </section>
{% endblock %}


