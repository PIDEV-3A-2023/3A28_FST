knp_paginator:
    page_range: 5                      # number of links showed in the pagination menu (e.g: you have 10 pages, a lot of pages will be showed, so only the 5 most relevants are showed)
    default_options:
        page_name: page                # page query parameter name
        sort_field_name: sort          # sort field query parameter name
        sort_direction_name: direction # sort direction query parameter name
        distinct: true                 # ensure distinct results, useful when ORM queries are using GROUP BY statements
    template:
        pagination: 'KnpPaginatorBundle:Pagination:twitter_bootstrap_v4_pagination.html.twig' # bootstrap 4 template
        sortable: 'KnpPaginatorBundle:Pagination:sortable_link.html.twig'